import os\nimport json\nimport tarfile\n\ndef split_reports(input_directory):\n    for filename in os.listdir(input_directory):\n        if filename.endswith('.json') or filename.endswith('.tar.gz'):  # Check for JSON and tar.gz files\n            file_path = os.path.join(input_directory, filename)\n            if filename.endswith('.json'):\n                with open(file_path, 'r') as json_file:\n                    data = json.load(json_file)\n                    # Process JSON file here\n                    print(f'Processing JSON file: {filename}')\n                    # ... (your processing logic)\n                \n            elif filename.endswith('.tar.gz'):\n                with tarfile.open(file_path, 'r:gz') as tar_file:\n                    tar_file.extractall(path=input_directory)\n                    # Process files extracted from tar.gz here\n                    print(f'Extracted and processed tar.gz file: {filename}')\n                    # ... (your processing logic)\n\n# Example usage\nin_directory = 'path/to/input/directory'  # Provide your input directory path here\nsplit_reports(in_directory)